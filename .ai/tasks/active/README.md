# Active Tasks

Keep one file per non-trivial task in progress.

When a task finishes, move or rewrite its final state into `../done/`.

When a task pauses unfinished, create or update a matching file in `../../handoffs/`.
