# Decision Records

Use this directory for ADR-style notes that future assistants should treat as durable guidance.

Create a new record when you introduce or change:

- architectural invariants
- shared workflow rules
- repository-wide engineering policies
- persistent integration assumptions

Keep one decision per file and use the numeric prefix format `0001-...`.
