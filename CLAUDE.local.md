## VPS Access

Все проекты MemeLab размещены на одном VPS. Для подключения:

```bash
ssh -i ~/.ssh/memelab_vps root@memelab.ru
```

Используй это подключение для деплоя, просмотра логов, перезапуска сервисов и других операций на сервере.
